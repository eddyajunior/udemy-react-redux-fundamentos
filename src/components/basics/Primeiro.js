import React from 'react';

const Primeiro = () => {
    const msg = "Seja bem vindo!";

    return (
        <div>
            Meu Primeiro Componente
            <h2>{msg}</h2>
        </div>
    );
};

export default Primeiro;